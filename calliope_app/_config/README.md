# About _config

## XPRESS setup
This `_config` diretory previously was used to configure `XPRESS` solver with static licenses stored on AWS Parameter store.

## Nginx `default.config` kept

Engage is using custom nginx settings, not default to Stratus Cloud team's generic settings.
